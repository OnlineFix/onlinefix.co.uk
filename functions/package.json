{
  "name": "onlinefix-functions",
  "version": "1.0.0",
  "description": "Cloud Functions for the onlinefix.co.uk booking and availability system",
  "main": "index.js",
  "private": true,
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "node --test test/"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.1",
    "googleapis": "^144.0.0"
  }
}
